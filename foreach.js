
let a = ['juan', 'isabella', 'valeria', 'jose'];
a.forEach(function(element) {
    console.log(element);
}
)

let soli = 'to be or no to be';
Array.from(soli).forEach(function(character) {
    console.log(character);
}
)

a.forEach(element => {
    console.log(element);    
});